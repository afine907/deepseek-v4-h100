# H100 生产环境配置
# 切换方式: cp configs/model.h100.yaml configs/model.yaml
model:
  backend: vllm
  name: deepseek-ai/DeepSeek-V4-Flash
  tensor_parallel_size: 8
  dtype: float16
  quantization: fp8
  max_model_len: 8192
  max_num_seqs: 32
  gpu_memory_utilization: 0.90
